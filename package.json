{
  "name": "nissi-site",
  "version": "1.0.0",
  "private": true,
  "description": "Site NISSI - statique + API contact (Resend + Postgres)",
  "dependencies": {
    "@vercel/blob": "^2.6.1",
    "resend": "^4.5.1"
  },
  "scripts": {
    "dev": "python3 -m http.server 8321 --bind 127.0.0.1",
    "dev:api": "vercel dev"
  }
}